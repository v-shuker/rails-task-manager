<h1>Tasks</h1>

<ul style="list-style-type:none">
<% @tasks.each do |task| %>
  <% check = task.completed ? "✅ " : "⛔ " %>
  <li>
    <%= check %><%= link_to "#{task[:title]}", task_path(task[:id]) %><%= " • " %><%= link_to "🗑", delete_task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
  </li>
<% end %>
</ul>

<%= link_to "+ Add a new task", new_path %>
